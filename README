We are analyzing US gun murder data collected by the FBI

download-data <- downloads csv and saves it to directory

wrangle-data <- creates a derived data set and saves it as an rda directory

analyze <- generates a plot and saves it to figs directory